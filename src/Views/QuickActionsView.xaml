<Window x:Class="XcTools.Views.QuickActionsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        d:DesignHeight="400"
        d:DesignWidth="600"
        Title="小辰CAD工具箱 - 快捷操作"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 绘制命令组 -->
        <GroupBox Grid.Row="0" Header="绘制" Margin="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Content="直线\nLINE" Grid.Column="0" Margin="10" Click="LineButton_Click"/>
                <Button Content="矩形\nRECTANG" Grid.Column="1" Margin="10" Click="RectangleButton_Click"/>
                <Button Content="圆\nCIRCLE" Grid.Column="2" Margin="10" Click="CircleButton_Click"/>
            </Grid>
        </GroupBox>
        
        <!-- 修改命令组 -->
        <GroupBox Grid.Row="1" Header="修改" Margin="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Content="删除\nERASE" Grid.Column="0" Margin="10" Click="EraseButton_Click"/>
                <Button Content="复制\nCOPY" Grid.Column="1" Margin="10" Click="CopyButton_Click"/>
                <Button Content="移动\nMOVE" Grid.Column="2" Margin="10" Click="MoveButton_Click"/>
            </Grid>
        </GroupBox>
        
        <!-- 视图命令组 -->
        <GroupBox Grid.Row="2" Header="视图" Margin="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Content="平移\nPAN" Grid.Column="0" Margin="10" Click="PanButton_Click"/>
                <Button Content="窗口缩放\nZOOM W" Grid.Column="1" Margin="10" Click="ZoomWindowButton_Click"/>
                <Button Content="全部缩放\nZOOM E" Grid.Column="2" Margin="10" Click="ZoomExtentsButton_Click"/>
            </Grid>
        </GroupBox>
        
        <!-- 其他命令组 -->
        <GroupBox Grid.Row="3" Header="其他" Margin="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Content="修剪\nTRIM" Grid.Column="0" Margin="10" Click="TrimButton_Click"/>
                <Button Content="合并\nJOIN" Grid.Column="1" Margin="10" Click="JoinButton_Click"/>
                <Button Content="分解\nEXPLODE" Grid.Column="2" Margin="10" Click="ExplodeButton_Click"/>
            </Grid>
        </GroupBox>
        
        <!-- 状态栏 -->
        <StatusBar Grid.Row="5" Height="22">
            <StatusBarItem HorizontalContentAlignment="Left">
                <TextBlock Text="就绪"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>